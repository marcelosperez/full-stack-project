services:
  react19-app:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3001:5173"
    environment:
      - WATCHPACK_POLLING=true
    command: sh -c "npm install && npm run dev -- --host"